gradio>=4.0.0
plotly>=5.0.0
torch>=2.0.0
numpy>=1.24.0
huggingface-hub>=0.19.0
gymnasium>=0.29.0
openai>=1.30.0
pyyaml>=6.0
python-dotenv>=1.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
# audioop-lts only needed for Python 3.13+ where stdlib audioop is gone.
# Our Docker image is python:3.11-slim, which still has audioop.
